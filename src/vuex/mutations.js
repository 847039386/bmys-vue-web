import store_mutations from './store_mutations.js'

export default {
  delProgress :store_mutations.delProgress,
  router :store_mutations.router,
  ModelPermission :store_mutations.ModelPermission,
  verifyAdmin :store_mutations.verifyAdmin,
  reqIsSuccess : store_mutations.reqIsSuccess
}
